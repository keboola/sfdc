[config]

app.env = production
app.admin =
app.email =
app.name =
app.projectName =
app.url =

salesForce.clientId =
salesForce.clientSecret =
salesForce.redirectUri =
salesForce.loginUri = https://login.salesforce.com

gooddata.username =
gooddata.password =

db.host = localhost
db.name =
db.login =
db.password =
